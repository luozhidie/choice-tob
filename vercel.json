{
  "version": 2,
  "name": "choice-tob",
  "buildCommand": "cd frontend && pnpm install && pnpm build",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://choice-tob-api.vercel.app/api/$1" }
  ]
}
